[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "workers-runtime-sdk>=1.6.8",
]

[dependency-groups]
dev = [
    "workers-py>=1.15.0",
    "workers-runtime-sdk",
]
